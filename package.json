{
  "name": "@aztec-pioneers/zcash-swaps",
  "workspaces": {
    "packages": [
      "solver",
      "web-app"
    ],
    "catalog": {
      "@types/bun": "latest",
      "typescript": "~5.9.3",
      "vite": "^7.1.7"
    }
  },
  "scripts": {
    "orchestrate": "docker compose down -v && docker compose --env-file .env up --build",
    "mint": "docker compose exec solver bun run scripts/mint_tokens.ts"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}