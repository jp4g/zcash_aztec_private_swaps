{
  "name": "@aztec-pioneers/zcash-swaps",
  "workspaces": {
    "packages": [
      "solver",
      "web-app"
    ],
    "catalog": {
      "@types/bun": "latest",
      "typescript": "~5.9.3",
      "vite": "^7.1.7"
    }
  },
  "scripts": {
    "after": "make init && make build-deps && make build-token && make build-contract",
    "orchestrate": "docker compose down -v && docker compose --env-file .env up --build"
  },
  "dependencies": {
    "dotenv": "^17.2.3"
  }
}